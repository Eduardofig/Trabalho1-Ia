#pragma once

#include <cmath>
#include <vector>
#include <utility>
#include <algorithm>

namespace aux
{
    double dist(const int from, const int to, const std::vector<std::pair<int, int>> &vertices);
}
